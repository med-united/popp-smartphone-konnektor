/** Please modify this class to meet your needs This class is not complete */
package de.gematik.ws.conn.eventservice.wsdl.v7_2;

import com.google.common.base.Strings;
import de.gematik.ws.conn.cardservice.v8.CardInfoType;
import de.gematik.ws.conn.cardservicecommon.v2.CardTypeType;
import de.gematik.ws.conn.connectorcommon.v5.Status;
import de.gematik.ws.conn.eventservice.v7.GetCardsResponse;
import de.gematik.ws.conn.eventservice.v7.GetSubscriptionResponse;
import de.gematik.ws.conn.eventservice.v7.RenewSubscriptionsResponse;
import de.gematik.ws.conn.eventservice.v7.SubscribeResponse;
import de.gematik.ws.conn.eventservice.v7.SubscriptionRenewal;
import de.gematik.ws.conn.eventservice.v7.SubscriptionType;
import de.gematik.ws.conn.eventservice.v7.UnsubscribeResponse;
import de.servicehealth.popp.session.Store;
import de.servicehealth.popp.session.WebsocketEntry;
import io.quarkus.logging.Log;
import io.quarkus.security.identity.SecurityIdentity;
import jakarta.inject.Inject;
import java.io.IOException;
import java.math.BigInteger;
import java.time.Instant;
import java.time.ZoneOffset;
import java.time.temporal.ChronoUnit;
import java.util.ArrayList;
import java.util.GregorianCalendar;
import java.util.List;
import java.util.Optional;
import java.util.UUID;
import java.util.logging.Logger;
import javax.naming.InvalidNameException;
import javax.naming.ldap.LdapName;
import javax.naming.ldap.Rdn;
import javax.xml.datatype.DatatypeConfigurationException;
import javax.xml.datatype.DatatypeConstants;
import javax.xml.datatype.DatatypeFactory;
import javax.xml.datatype.XMLGregorianCalendar;
import org.bouncycastle.asn1.ASN1Primitive;
import org.bouncycastle.asn1.ASN1String;
import org.eclipse.microprofile.config.inject.ConfigProperty;

/**
 * This class was generated by Apache CXF 4.1.4 2026-01-20T23:45:08.791+01:00 Generated source
 * version: 4.1.4
 */
@jakarta.jws.WebService(
    serviceName = "EventService",
    portName = "EventServicePort",
    targetNamespace = "http://ws.gematik.de/conn/EventService/WSDL/v7.2",
    wsdlLocation = "classpath:/wsdl/EventService.wsdl",
    endpointInterface = "de.gematik.ws.conn.eventservice.wsdl.v7_2.EventServicePortType")
public class EventServicePortImpl implements EventServicePortType {

  @Inject SecurityIdentity identity; // Inject the SecurityIdentity

  @Inject Store store;

  @Inject Subscriptions subscriptions;

  @ConfigProperty(name = "popp.smcb-handle")
  String smcbHandle;

  private static final Logger LOG = Logger.getLogger(EventServicePortImpl.class.getName());

  /* (non-Javadoc)
   * @see de.gematik.ws.conn.eventservice.wsdl.v7_2.EventServicePortType#subscribe(de.gematik.ws.conn.eventservice.v7.Subscribe parameter)*
   */
  public de.gematik.ws.conn.eventservice.v7.SubscribeResponse subscribe(
      de.gematik.ws.conn.eventservice.v7.Subscribe parameter) throws FaultMessage {

    var tlsCertCn = getTlsCertCN();
    try {
      LOG.info("Executing operation subscribe");
      var exisitingSubscription =
          subscriptions.findSubscription(
              getTlsCertCN(),
              sub ->
                  (sub.getEventTo() == null
                          || sub.getEventTo().equals(parameter.getSubscription().getEventTo()))
                      && (sub.getTopic() == null
                          || sub.getTopic().equals(parameter.getSubscription().getTopic()))
                      && (sub.getFilter() == null
                          || sub.getFilter().equals(parameter.getSubscription().getFilter())));
      if (exisitingSubscription.isEmpty()) {
        var newSubscriptionId = UUID.randomUUID().toString();

        Log.warn(
            "subscription does already exist. Not added. Adding with ID: " + newSubscriptionId);

        var parameterSubscription = parameter.getSubscription();
        // We have to set a termination time, even if none is passed. Otherwise some Primary Systems
        // will fail when retrieving the subscription
        if (parameterSubscription.getTerminationTime() == null) {
          XMLGregorianCalendar xmlCal = getTimeInOneHour();
          parameterSubscription.setTerminationTime(xmlCal);
        }
        parameter.getSubscription().setSubscriptionID(newSubscriptionId);
        parameter
            .getSubscription()
            .setTerminationTime(parameter.getSubscription().getTerminationTime());
        subscriptions.addSubscription(tlsCertCn, parameter.getSubscription());
      }
      de.gematik.ws.conn.eventservice.v7.SubscribeResponse _return = new SubscribeResponse();
      _return.setStatus(new Status());
      _return.getStatus().setResult("OK");
      _return.setSubscriptionID(parameter.getSubscription().getSubscriptionID());
      _return.setTerminationTime(parameter.getSubscription().getTerminationTime());
      return _return;
    } catch (java.lang.Exception ex) {
      ex.printStackTrace();
      throw new FaultMessage("FaultMessage...", ex);
    }
  }

  private List<SubscriptionType> getSubscriptions() {
    String tlsCertCN = getTlsCertCN();
    String cardHandle = getSMCBCardHandle();
    LOG.fine("Authenticated user: " + identity.getPrincipal().getName());

    List<SubscriptionType> subscriptionTypes = subscriptions.getSubscriptions(tlsCertCN);

    // purge terminated subscriptions
    try {
      XMLGregorianCalendar now =
          DatatypeFactory.newInstance()
              .newXMLGregorianCalendar(
                  GregorianCalendar.from(Instant.now().atZone(ZoneOffset.UTC)));

      subscriptions.removeSubscription(
          tlsCertCN,
          (subscription) ->
              subscription.getTerminationTime().compare(now) == DatatypeConstants.LESSER);

    } catch (DatatypeConfigurationException e) {
      throw new RuntimeException(e);
    }

    return subscriptionTypes;
  }

  /* (non-Javadoc)
   * @see de.gematik.ws.conn.eventservice.wsdl.v7_2.EventServicePortType#getResourceInformation(de.gematik.ws.conn.eventservice.v7.GetResourceInformation parameter)*
   */
  public de.gematik.ws.conn.eventservice.v7.GetResourceInformationResponse getResourceInformation(
      de.gematik.ws.conn.eventservice.v7.GetResourceInformation parameter) throws FaultMessage {
    LOG.info("Executing operation getResourceInformation");
    System.out.println(parameter);
    LOG.fine("Authenticated user: " + identity.getPrincipal().getName());
    try {
      return null;
    } catch (java.lang.Exception ex) {
      ex.printStackTrace();
      throw new FaultMessage("FaultMessage...", ex);
    }
    // throw new FaultMessage("FaultMessage...");
  }

  /* (non-Javadoc)
   * @see de.gematik.ws.conn.eventservice.wsdl.v7_2.EventServicePortType#getSubscription(de.gematik.ws.conn.eventservice.v7.GetSubscription parameter)*
   */
  public de.gematik.ws.conn.eventservice.v7.GetSubscriptionResponse getSubscription(
      de.gematik.ws.conn.eventservice.v7.GetSubscription parameter) throws FaultMessage {
    LOG.info("Executing operation getSubscription");
    try {
      de.gematik.ws.conn.eventservice.v7.GetSubscriptionResponse _return =
          new GetSubscriptionResponse();

      List<SubscriptionType> subscription =
          getSubscriptions().stream()
              .filter(
                  sub -> {
                    if (Strings.isNullOrEmpty(parameter.getSubscriptionID())) {
                      return true;
                    } else {
                      return sub.getSubscriptionID().equals(parameter.getSubscriptionID());
                    }
                  })
              .toList();

      _return.setStatus(new Status());
      _return.getStatus().setResult("OK");
      _return.setSubscriptions(new GetSubscriptionResponse.Subscriptions());

      for (SubscriptionType subscriptionType : subscription) {
        _return.getSubscriptions().getSubscription().add(subscriptionType);
      }

      return _return;
    } catch (java.lang.Exception ex) {
      ex.printStackTrace();
      throw new FaultMessage("FaultMessage...", ex);
    }
    // throw new FaultMessage("FaultMessage...");
  }

  /* (non-Javadoc)
   * @see de.gematik.ws.conn.eventservice.wsdl.v7_2.EventServicePortType#unsubscribe(de.gematik.ws.conn.eventservice.v7.Unsubscribe parameter)*
   */
  public de.gematik.ws.conn.eventservice.v7.UnsubscribeResponse unsubscribe(
      de.gematik.ws.conn.eventservice.v7.Unsubscribe parameter) throws FaultMessage {
    LOG.info("Executing operation unsubscribe");
    try {
      getSubscriptions()
          .removeIf(sub -> sub.getSubscriptionID().equals(parameter.getSubscriptionID()));
      de.gematik.ws.conn.eventservice.v7.UnsubscribeResponse _return = new UnsubscribeResponse();
      return _return;
    } catch (java.lang.Exception ex) {
      ex.printStackTrace();
      throw new FaultMessage("FaultMessage...", ex);
    }
    // throw new FaultMessage("FaultMessage...");
  }

  /* (non-Javadoc)
   * @see de.gematik.ws.conn.eventservice.wsdl.v7_2.EventServicePortType#getCardTerminals(de.gematik.ws.conn.eventservice.v7.GetCardTerminals parameter)*
   */
  public de.gematik.ws.conn.eventservice.v7.GetCardTerminalsResponse getCardTerminals(
      de.gematik.ws.conn.eventservice.v7.GetCardTerminals parameter) throws FaultMessage {
    LOG.info("Executing operation getCardTerminals");
    System.out.println(parameter);
    try {
      de.gematik.ws.conn.eventservice.v7.GetCardTerminalsResponse _return = null;
      return _return;
    } catch (java.lang.Exception ex) {
      ex.printStackTrace();
      throw new RuntimeException(ex);
    }
    // throw new FaultMessage("FaultMessage...");
  }

  /* (non-Javadoc)
   * @see de.gematik.ws.conn.eventservice.wsdl.v7_2.EventServicePortType#renewSubscriptions(de.gematik.ws.conn.eventservice.v7.RenewSubscriptions parameter)*
   */
  public de.gematik.ws.conn.eventservice.v7.RenewSubscriptionsResponse renewSubscriptions(
      de.gematik.ws.conn.eventservice.v7.RenewSubscriptions parameter) throws FaultMessage {
    LOG.info("Executing operation renewSubscriptions");
    System.out.println(parameter);

    List<SubscriptionRenewal> renewals = new ArrayList<>();
    XMLGregorianCalendar inOneHour;

    try {
      inOneHour = getTimeInOneHour();
    } catch (DatatypeConfigurationException ex) {
      ex.printStackTrace();
      throw new FaultMessage(ex.getMessage());
    }

    for (String id : parameter.getSubscriptionID()) {
      var subscription =
          getSubscriptions().stream()
              .filter(subscriptionType -> subscriptionType.getSubscriptionID().equals(id))
              .findFirst();

      if (subscription.isEmpty()) {
        continue;
      }

      subscription.get().setTerminationTime(inOneHour);

      var renewal = new SubscriptionRenewal();
      renewal.setSubscriptionID(id);
      renewal.setTerminationTime(inOneHour);
      renewals.add(renewal);
    }

    var response = new RenewSubscriptionsResponse();
    var subscribeRenewals = new RenewSubscriptionsResponse.SubscribeRenewals();
    response.setSubscribeRenewals(subscribeRenewals);
    response.getSubscribeRenewals().getSubscriptionRenewal().addAll(renewals);
    var status = new Status();
    status.setResult("OK");
    response.setStatus(status);

    return response;
  }

  /* (non-Javadoc)
   * @see de.gematik.ws.conn.eventservice.wsdl.v7_2.EventServicePortType#getCards(de.gematik.ws.conn.eventservice.v7.GetCards parameter)*
   */
  public de.gematik.ws.conn.eventservice.v7.GetCardsResponse getCards(
      de.gematik.ws.conn.eventservice.v7.GetCards parameter) {
    LOG.info("Executing operation getCards");
    System.out.println(parameter);
    var cardTypeParameter = Optional.ofNullable(parameter.getCardType());
    String tlsCertCN = getTlsCertCN();
    String cardHandle = getSMCBCardHandle();
    LOG.fine("Authenticated user: " + identity.getPrincipal().getName());

    de.gematik.ws.conn.eventservice.v7.GetCardsResponse _return = new GetCardsResponse();
    _return.setCards(new de.gematik.ws.conn.cardservice.v8.Cards());
    _return.setStatus(new de.gematik.ws.conn.connectorcommon.v5.Status());

    if (cardTypeParameter.map(card -> card == CardTypeType.EGK).orElse(true)) {
      List<WebsocketEntry> entries = store.getEntriesOfCertCN(tlsCertCN);
      _return
          .getCards()
          .getCard()
          .addAll(entries.stream().map(WebsocketEntry::getCardInfoType).toList());
    }

    if (cardTypeParameter.map(card -> card == CardTypeType.SMC_B).orElse(true)) {
      // In order to make ere ps app happy we need to tell it that an smcb actually exists
      var smcb = new CardInfoType();
      smcb.setCardHandle(cardHandle);
      smcb.setCardType(CardTypeType.SMC_B);
      smcb.setInsertTime(WebsocketEntry.nowAsXMLGregorianCalendar());
      smcb.setSlotId(BigInteger.valueOf(1L));
      _return.getCards().getCard().add(smcb);
    }

    _return.getStatus().setResult("OK");
    return _return;
  }

  private String getTlsCertCN() {
    LdapName ldapName;
    try {
      ldapName = new LdapName(identity.getPrincipal().getName());
    } catch (InvalidNameException e) {
      throw new RuntimeException(e);
    }
    String cn =
        ldapName.getRdns().stream()
            .filter(rdn -> rdn.getType().equalsIgnoreCase("CN"))
            .map(rdn -> rdn.getValue().toString())
            .findFirst()
            .orElseThrow();
    return cn.isEmpty() ? null : cn.replace("CN=", "");
  }

  private String getSMCBCardHandle() {
    try {
      var ldapName = new LdapName(identity.getPrincipal().getName());
      Object raw =
          ldapName.getRdns().stream()
              .filter(rdn -> rdn.getType().equals("2.5.4.5"))
              .map(Rdn::getValue)
              .findFirst()
              .orElseThrow();
      byte[] bytes = (byte[]) raw;
      return ((ASN1String) ASN1Primitive.fromByteArray(bytes)).getString();
    } catch (InvalidNameException | IOException e) {
      throw new RuntimeException(e);
    }
  }

  private XMLGregorianCalendar getTimeInOneHour() throws DatatypeConfigurationException {
    return DatatypeFactory.newInstance()
        .newXMLGregorianCalendar(
            GregorianCalendar.from(Instant.now().plus(1, ChronoUnit.HOURS).atZone(ZoneOffset.UTC)));
  }
}
